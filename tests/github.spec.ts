/**
 * git hub
 * bit bucket
 * bit lab
 * these are version control systems
 * 
 * 
 * after creating   repository
 * 
 * 1.git init
 * 2 git add .
 * 3 git commit -m "first commit"
 * 4 git branch -M main
 * 5 git remote add origin https://github.com/harithathirunahari7-rgb/NITTSFRAMEWORK.git
 * 6 git push -u origin main
 * now the main branch has been created
 * now clone your git repository to your local machine
 * git clone https://github.com/harithathirunahari7-rgb/NITTSFRAMEWORK.git
 * after clone is done run npm install --must
 * now make some changes to the code in your local system
 * first create the new branch inside the local system
 * git checkout -b <branchname>
 * git checkout -b feature-branch  // to switch to the branch
 * git brach  // to check the current branch
 * git status
 * git add .
 * git commit -m "new changes"
 * git push 
 * error
 * git push --set-upstream origin feature-branch// not coming bacause i did it in the browser
 * git push
 * add new changes to the feature branch
 * 
 * 
 * create pull request ok
 * 
 * */